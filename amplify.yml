version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site — no build required"
    build:
      commands:
        - echo "Publishing static files as-is"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
